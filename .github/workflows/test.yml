name: Test

on:
  pull_request:
    branches: [main, beta]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository.
        uses: actions/checkout@v2
        with:
          submodules: 'true'
          token: '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'
      - uses: actions/setup-node@v2
        with:
          node-version: '16'
      - name: Reconfigure git to use HTTP authentication
        run: >
          git config --global url."https://github.com/".insteadOf
          ssh://git@github.com/
      - name: Install modules.
        run: yarn
      - name: Test
        run: yarn test:ci
